# Mnemosyne

Local memory palace: retrieval-augmented generation and semantic search, fully on your machine.

Work lands in three stacked pull requests:

1. Local RAG engine (`rag/`)
2. Django API (`backend/`)
3. React oracle (`frontend/`)
